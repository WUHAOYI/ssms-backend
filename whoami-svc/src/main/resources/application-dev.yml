server:
  port: 8084

spring:
  jpa:
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

ssms:
  account-service-endpoint: http://account-svc:8081
  company-service-endpoint: http://company-svc:8082